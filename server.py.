from fastapi import FastAPI
app = FastAPI()

@app.get("/")
async def root():
    return {"message": "V3gasboi.online is FRESH and LIVE!"}

@app.on_event("shutdown")
async def shutdown_db_client():
    # This 'pass' fixes the NameError you had earlier!
    pass 
